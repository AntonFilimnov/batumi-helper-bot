services:
  bot-app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - bot-net

networks:
  bot-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/24
    enable_ipv6: false